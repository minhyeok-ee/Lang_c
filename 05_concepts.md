# 05. 개념 서술형 / 면접 질문

필기의 단답형·서술형 문제나 실무 면접에서 말로 설명해야 하는 질문입니다.
**답을 보기 전에 소리 내어 1분 안에 설명하거나 종이에 3~4줄로 적어보세요.**
정답과 해설: `solutions/05_answers.md`

---

## A. C 언어 키워드

1. `volatile` 키워드는 무엇이고, 펌웨어에서 꼭 써야 하는 경우 3가지는?
2. `static`이 (a) 함수 안의 지역 변수, (b) 파일 범위의 전역 변수, (c) 함수 앞에 붙을 때 각각 의미는?
3. 다음 네 선언의 차이를 설명하세요.
   ```c
   const int *p1;
   int const *p2;
   int *const p3;
   const int *const p4;
   ```
4. `const`와 `volatile`을 함께 쓸 수 있나요? 예를 들어 설명하세요.
5. `extern` 키워드의 역할은? 헤더 파일에 전역 변수를 정의(`int g = 0;`)하면 어떤 문제가 생기나요?
6. `#define`으로 만든 상수/매크로와 `const` 변수, `inline` 함수의 차이는?

## B. 메모리

7. C 프로그램의 메모리 영역(text, data, bss, heap, stack)에 각각 무엇이 저장되나요?
   아래 변수들은 어디에 있을까요?
   ```c
   int a = 10;
   int b;
   static int c;
   const char *msg = "hello";
   void f(void) {
       int d;
       static int e = 1;
       int *h = malloc(4);
   }
   ```
8. 임베디드 펌웨어에서 `malloc` 사용을 피하는 이유는?
9. 스택 오버플로가 발생하는 원인과 임베디드 환경에서 탐지하는 방법은?
10. 메모리 누수, 댕글링 포인터, 버퍼 오버플로를 각각 설명하세요.
11. 빅 엔디안과 리틀 엔디안의 차이는? 통신 프로토콜에서 왜 중요한가요?

## C. 인터럽트와 동시성

12. 인터럽트 서비스 루틴(ISR)을 작성할 때 주의할 점은? (최소 4가지)
13. ISR 안에서 `printf`나 `malloc`을 호출하면 안 되는 이유는?
14. 폴링(polling) 방식과 인터럽트 방식의 장단점은?
15. 경쟁 조건(race condition)이란? 메인 루프와 ISR이 같은 변수를 쓸 때 막는 방법은?
16. 뮤텍스와 세마포어의 차이는? 우선순위 역전(priority inversion)이란?
17. 워치독 타이머(watchdog)의 역할과, 올바르게 "킥(kick)"하는 위치는?

## D. 통신과 하드웨어

18. UART, I2C, SPI를 선 개수, 속도, 통신 방식(동기/비동기), 다중 장치 연결 관점에서 비교하세요.
19. I2C에서 풀업 저항이 필요한 이유는? ACK/NACK와 clock stretching이란?
20. I2C 통신이 안 될 때 어떤 순서로 원인을 찾겠습니까?
21. PWM이란? 모니터에서 백라이트 밝기를 PWM으로 조절하는 원리는?
22. 오픈 드레인(open-drain) 출력과 푸시풀(push-pull) 출력의 차이는?

## E. 모니터 펌웨어

23. EDID란 무엇이고, 어떤 경로로 PC에 전달되나요?
24. HDMI 케이블을 연결했을 때 화면이 나오기까지의 과정을 설명하세요.
25. 패널 전원 시퀀스(power sequence)에서 켤 때와 끌 때 순서가 중요한 이유는?
26. 스케일러(scaler) IC의 역할은?
27. "화면이 가끔 깜빡인다"는 불량이 들어왔습니다. 펌웨어 엔지니어로서 어떻게 분석하겠습니까?
